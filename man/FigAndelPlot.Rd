% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/FigAndelPlot.R
\name{FigAndelPlot}
\alias{FigAndelPlot}
\title{Horisontalt andelsplott for true/false-variabler}
\usage{
FigAndelPlot(
  data,
  andelVariabel,
  gruppeVariabel = NULL,
  terskel = 5,
  totalLabel = "Totalt",
  trueValues = NULL,
  falseValues = NULL,
  tittel = NULL,
  breaks = NULL,
  highGood = TRUE
)
}
\arguments{
\item{data}{Data frame med inputdata.}

\item{andelVariabel}{Navn på variabel med true/false-verdier.}

\item{gruppeVariabel}{Valgfritt navn på grupperingsvariabel. Hvis `NULL`
vises kun total-rad.}

\item{terskel}{Nedre grense for gyldig antall per gruppe. Grupper under
terskel vises uten stolpe. Standard: `5`.}

\item{totalLabel}{Navn på total-rad. Standard: `"Totalt"`.}

\item{trueValues}{Valgfri vektor med ekstra verdier som skal tolkes som
TRUE.}

\item{falseValues}{Valgfri vektor med ekstra verdier som skal tolkes som
FALSE.}

\item{tittel}{Valgfri tittel for figuren. Standard: `NULL`.}

\item{breaks}{Valgfri vektor med to prosentgrenser for kvalitetsbånd i
bakgrunnen, for eksempel `c(75, 90)`. Hvis `NULL` tegnes ingen bånd.}

\item{highGood}{Logisk. `TRUE` betyr at høy andel er bra. `FALSE` betyr at
lav andel er bra. Standard: `TRUE`.}
}
\value{
Et `ggplot2`-objekt.
}
\description{
Lager et horisontalt stolpediagram som viser andel TRUE per gruppe,
beregnet som TRUE / (TRUE + FALSE). Manglende verdier i andelsvariabel
fjernes før beregning.
}
\examples{
df <- data.frame(
  gruppe = c("A", "A", "B", "B", "C", "C", "C", "D", "D"),
  verdi = c(TRUE, FALSE, TRUE, TRUE, FALSE, FALSE, TRUE, NA, TRUE)
)

FigAndelPlot(
  data = df,
  andelVariabel = "verdi",
  gruppeVariabel = "gruppe"
)

FigAndelPlot(
  data = df,
  andelVariabel = "verdi",
  gruppeVariabel = "gruppe",
  tittel = "Andel TRUE per gruppe",
  breaks = c(75, 90),
  highGood = TRUE
)
}
